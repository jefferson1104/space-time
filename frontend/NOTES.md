```bash
# create next app with typescript, tailwindcss, eslint, app router and import alias.
$ npx create-next-app@latest frontend --use-npm

# install rocketseat eslint config
$ npm i @rocketseat/eslint-config -D

# install prettier tailwindcss plugin
$ npm i prettier-plugin-tailwindcss -D
```
